# Core web framework
falcon==4.0.2

# WSGI server (single worker for SQLite safety)
gunicorn==23.0.0

# JWT handling
PyJWT==2.10.1

# Google OAuth token exchange (no heavy oauth lib needed — pure stdlib)
# urllib.request is used directly for minimal footprint

# LXD API client (Linux only — not installed on Windows dev; mocked instead)
# Uncomment on Linux/WSL:
pylxd==2.4.1

# TSDB and Database
# sqlite3 is built-in

# Environment variable loading
python-dotenv==1.0.1

